# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
channels:
  - conda-forge
  - bioconda
dependencies:
  # Same GATK build as modules/local/gatk4/grouped_sv_cluster and
  # modules/nf-core/gatk4/svcluster -- CollectReadCounts is a different
  # walker in the same gatk4-main jar, not a separate tool.
  - bioconda::gatk4-main=4.7.0.0
  - bioconda::gcnvkernel=0.9
