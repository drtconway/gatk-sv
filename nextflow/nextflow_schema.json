{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/broadinstitute/gatk-sv/main/nextflow/nextflow_schema.json",
    "title": "gatk-sv-nextflow pipeline parameters",
    "description": "Nextflow reimplementation of the GATK-SV harmonisation pipeline. See nextflow/README.md.",
    "type": "object",
    "properties": {
        "input": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "pattern": "^\\S+\\.tsv$",
            "description": "Sample sheet TSV (family_id, sample_id, bam, bai). See README.md#sample-sheet.",
            "schema": "assets/schema_samplesheet.json"
        },
        "fasta": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Reference genome FASTA (hg38)."
        },
        "fasta_fai": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Reference genome FASTA index (.fai)."
        },
        "reference_dict": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Reference genome sequence dictionary (.dict). Required by clustering workflows (GATK4_SVCLUSTER)."
        },
        "primary_contigs_list": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "List of canonical contigs (one per line), used e.g. by Wham (-c). Required by workflows/call_wham.nf; not needed by callers that don't use it."
        },
        "ped": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Pedigree file for the whole run. See README.md#pedigree-file. Required by clustering workflows."
        },
        "pesr_exclude_intervals": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Blacklist BED of intervals to exclude from PE/SR clustering (centromeres etc.). Required by clustering workflows."
        },
        "pesr_exclude_intervals_tbi": {
            "type": "string",
            "format": "file-path",
            "exists": true,
            "description": "Tabix index for pesr_exclude_intervals."
        },
        "outdir": {
            "type": "string",
            "format": "directory-path",
            "description": "Output directory for published results.",
            "default": "./results"
        }
    },
    "required": ["input", "fasta", "fasta_fai"]
}
